--- src/webcraft/ui.py	2026-01-14 12:08:33.529211441 +0000
+++ src/webcraft/ui.py	2026-01-14 12:04:58.473569123 +0000
@@ -229,15 +220,6 @@
   <div class="topbar">
     <h1>Wagstaff WebCraft</h1>
     <a id="navCooking" class="navlink" href="#">Cooking</a>
-    <div class="small" style="display:flex; align-items:center; gap:6px;">
-      <span class="muted">Label</span>
-      <select id="labelMode">
-        <option value="en">EN</option>
-        <option value="zh">中文</option>
-        <option value="id">ID</option>
-      </select>
-    </div>
-
     <div class="search">
       <input id="q" type="text" placeholder="Search: axe | ing:twigs | tag:bookbuilder | filter:TOOLS | tab:LIGHT" />
       <button id="btnSearch" class="primary">Search</button>
@@ -306,10 +282,6 @@
       activeRecipeData: null,
       assets: {},
       icon: null, // {mode, static_base, api_base}
-    labelMode: localStorage.getItem('ws_label_mode') || 'en',
-      i18n: null, // public meta: {enabled, langs, ...}
-      i18nNames: {}, // {lang: {id: name}}
-      i18nLoaded: {}, // {lang: true}
     };
 
     function setError(msg) {
@@ -399,73 +371,13 @@
       }
     }
 
-
-    function getI18nName(iid, lang) {
-      const l = String(lang || '').trim().toLowerCase();
-      const mp = (state.i18nNames && state.i18nNames[l]) ? state.i18nNames[l] : null;
-      return mp && mp[iid] ? String(mp[iid]) : '';
-    }
-
-    async function ensureI18nNames(lang) {
-      const l = String(lang || '').trim().toLowerCase();
-      if (!l || l === 'en' || l === 'id') return;
-      if (state.i18nLoaded && state.i18nLoaded[l]) return;
-      try {
-        const res = await fetchJson(api(`/api/v1/i18n/names/${encodeURIComponent(l)}`));
-        state.i18nNames[l] = res.names || {};
-        state.i18nLoaded[l] = true;
-      } catch (e) {
-        state.i18nNames[l] = {};
-        state.i18nLoaded[l] = true;
-      }
-    }
-
-    function applyLabelModeUI() {
-      const sel = el('labelMode');
-      if (!sel) return;
-      const avail = (state.i18n && Array.isArray(state.i18n.langs)) ? state.i18n.langs : [];
-      const hasZh = avail.includes('zh');
-
-      const optZh = sel.querySelector('option[value="zh"]');
-      if (optZh) optZh.disabled = !hasZh;
-
-      if (!hasZh && state.labelMode === 'zh') state.labelMode = 'en';
-      sel.value = state.labelMode;
-    }
-
-    function resolveLabel(iid, m) {
-      const mode = String(state.labelMode || 'en').toLowerCase();
-      if (mode === 'id') return iid;
-      if (mode !== 'en') {
-        const loc = getI18nName(iid, mode);
-        if (loc) return loc;
-      }
-      return (m && m.name) ? m.name : iid;
-    }
-
-    async function setLabelMode(mode) {
-      const m = String(mode || 'en').trim().toLowerCase();
-      state.labelMode = (m === 'zh' || m === 'id' || m === 'en') ? m : 'en';
-      try { localStorage.setItem('ws_label_mode', state.labelMode); } catch (e) {}
-      await ensureI18nNames(state.labelMode);
-      applyLabelModeUI();
-
-      // re-render
-      try { renderRecipeList(); } catch (e) {}
-      try { renderRecipeDetail(state.activeRecipeData); } catch (e) {}
-    }
-
     function renderItem(id) {
       const iid = String(id || '').trim();
       if (!iid) return '';
       const m = (state.assets && state.assets[iid]) ? state.assets[iid] : null;
-      const name = resolveLabel(iid, m);
+      const name = (m && m.name) ? m.name : iid;
 
       const tipParts = [iid];
-      const en = (m && m.name) ? m.name : '';
-      const zh = getI18nName(iid, 'zh');
-      if (en) tipParts.push(`en:${en}`);
-      if (zh) tipParts.push(`zh:${zh}`);
       if (m && m.image) tipParts.push(m.image);
       if (m && m.atlas) tipParts.push(m.atlas);
       const tip = escHtml(tipParts.join(' | '));
@@ -562,13 +474,8 @@
       const m = await fetchJson(api('/api/v1/meta'));
       const sha = m.scripts_sha256_12 ? `sha:${m.scripts_sha256_12}` : '';
       el('meta').textContent = `${sha} | mode:${m.engine_mode || ''} | files:${m.scripts_file_count || ''}`;
-
-      // i18n (public meta)
-      state.i18n = m.i18n || null;
-      applyLabelModeUI();
     }
 
-
     async function loadGroups() {
       setError('');
       if (state.mode === 'filters') {
@@ -1625,73 +1513,13 @@
       }
     }
 
-
-    function getI18nName(iid, lang) {
-      const l = String(lang || '').trim().toLowerCase();
-      const mp = (state.i18nNames && state.i18nNames[l]) ? state.i18nNames[l] : null;
-      return mp && mp[iid] ? String(mp[iid]) : '';
-    }
-
-    async function ensureI18nNames(lang) {
-      const l = String(lang || '').trim().toLowerCase();
-      if (!l || l === 'en' || l === 'id') return;
-      if (state.i18nLoaded && state.i18nLoaded[l]) return;
-      try {
-        const res = await fetchJson(api(`/api/v1/i18n/names/${encodeURIComponent(l)}`));
-        state.i18nNames[l] = res.names || {};
-        state.i18nLoaded[l] = true;
-      } catch (e) {
-        state.i18nNames[l] = {};
-        state.i18nLoaded[l] = true;
-      }
-    }
-
-    function applyLabelModeUI() {
-      const sel = el('labelMode');
-      if (!sel) return;
-      const avail = (state.i18n && Array.isArray(state.i18n.langs)) ? state.i18n.langs : [];
-      const hasZh = avail.includes('zh');
-
-      const optZh = sel.querySelector('option[value="zh"]');
-      if (optZh) optZh.disabled = !hasZh;
-
-      if (!hasZh && state.labelMode === 'zh') state.labelMode = 'en';
-      sel.value = state.labelMode;
-    }
-
-    function resolveLabel(iid, m) {
-      const mode = String(state.labelMode || 'en').toLowerCase();
-      if (mode === 'id') return iid;
-      if (mode !== 'en') {
-        const loc = getI18nName(iid, mode);
-        if (loc) return loc;
-      }
-      return (m && m.name) ? m.name : iid;
-    }
-
-    async function setLabelMode(mode) {
-      const m = String(mode || 'en').trim().toLowerCase();
-      state.labelMode = (m === 'zh' || m === 'id' || m === 'en') ? m : 'en';
-      try { localStorage.setItem('ws_label_mode', state.labelMode); } catch (e) {}
-      await ensureI18nNames(state.labelMode);
-      applyLabelModeUI();
-
-      // re-render
-      try { renderRecipeList(); } catch (e) {}
-      try { renderRecipeDetail(state.activeRecipeData); } catch (e) {}
-    }
-
     function renderItem(id) {
       const iid = String(id || '').trim();
       if (!iid) return '';
       const m = (state.assets && state.assets[iid]) ? state.assets[iid] : null;
-      const name = resolveLabel(iid, m);
+      const name = (m && m.name) ? m.name : iid;
 
       const tipParts = [iid];
-      const en = (m && m.name) ? m.name : '';
-      const zh = getI18nName(iid, 'zh');
-      if (en) tipParts.push(`en:${en}`);
-      if (zh) tipParts.push(`zh:${zh}`);
       if (m && m.image) tipParts.push(m.image);
       if (m && m.atlas) tipParts.push(m.atlas);
       const tip = escHtml(tipParts.join(' | '));
@@ -1724,10 +1552,6 @@
       activeRecipeData: null,
       assets: {},
       icon: null, // {mode, static_base, api_base}
-    labelMode: localStorage.getItem('ws_label_mode') || 'en',
-      i18n: null, // public meta: {enabled, langs, ...}
-      i18nNames: {}, // {lang: {id: name}}
-      i18nLoaded: {}, // {lang: true}
     };
 
     function renderGroupList() {
@@ -1840,13 +1664,8 @@
       const m = await fetchJson(api('/api/v1/meta'));
       const sha = m.scripts_sha256_12 ? `sha:${m.scripts_sha256_12}` : '';
       el('meta').textContent = `${sha} | mode:${m.engine_mode || ''} | files:${m.scripts_file_count || ''}`;
-
-      // i18n (public meta)
-      state.i18n = m.i18n || null;
-      applyLabelModeUI();
     }
 
-
     async function loadGroups() {
       setError('');
 
